import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "F4F Exchange - Grow Your Twitter Followers",
  description:
    "Join the Twitter Follow-for-Follow exchange. Connect with real users and grow your following organically.",
  keywords: "Twitter, followers, F4F, follow for follow, social media growth",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
